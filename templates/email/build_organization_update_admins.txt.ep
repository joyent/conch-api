Hello,

<%= $user->name %> (<%= $user->email %>) modified the "<%= $organization %>" organization's
access to the "<%= $build %>" build at Joyent Conch (https://<%= host %>) to the "<%= $role %>" role.

Thank you,
Joyent Build Ops Team
