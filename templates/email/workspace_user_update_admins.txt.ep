Hello,

<%# TODO: we should specify the conch URL here %>\
<%= $user->name %> (<%= $user->email %>) has modified a user's access to the "<%= $workspace %>" workspace at Joyent Conch.
<%= $target_user->name %> (<%= $target_user->email %>) now has the "<%= $role %>" role.

Thank you,
Joyent Build Ops Team
