Hello,

<%# TODO: we should specify the conch URL here %>\
<%= $user->name %> (<%= $user->email %>) removed <%= $target_user->name %> (<%= $target_user->email %>) from the
"<%= $organization %>" organization at Joyent Conch.

Thank you,
Joyent Build Ops Team
