Hello,

<%# TODO: we should specify the conch URL here %>\
<%= $user->name %> (<%= $user->email %>) added <%= $target_user->name %> (<%= $target_user->email %>) to the
"<%= $build %>" build at Joyent Conch with the "<%= $role %>" role.

Thank you,
Joyent Build Ops Team
