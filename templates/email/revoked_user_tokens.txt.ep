Hello,

The following tokens at Joyent Conch (https://<%= host %>) have been reset:

<%= join("\n", map '    '.$_, $token_names->@*) %>

You should now log into https://<%= host %> using your login credentials.

Thank you,
Joyent Build Ops Team
